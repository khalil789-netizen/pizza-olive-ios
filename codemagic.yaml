workflows:
  ios_release:
    name: iOS Release (Capacitor)
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      node: latest
      xcode: latest
      vars:
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
        BUNDLE_ID: "com.pizzaolive.app"

    scripts:
      - name: Install NPM deps
        script: |
          npm ci || npm i

      - name: Capacitor sync iOS
        script: |
          npx cap sync ios

      - name: Install CocoaPods
        script: |
          cd ios/App
          pod install

      - name: Build IPA (unsigned for now)
        script: |
          cd ios/App
          xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -sdk iphoneos -archivePath build/App.xcarchive archive

    artifacts:
      - ios/App/build/**/*.xcarchive
      - ios/App/build/**/*.app